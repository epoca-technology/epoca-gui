import { Injectable } from '@angular/core';
import { IKerasRegressionService } from './interfaces';

@Injectable({
  providedIn: 'root'
})
export class KerasRegressionService implements IKerasRegressionService {

	constructor() { }
}
