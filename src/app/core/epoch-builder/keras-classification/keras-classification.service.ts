import { Injectable } from '@angular/core';
import { IKerasClassificationService } from './interfaces';

@Injectable({
  providedIn: 'root'
})
export class KerasClassificationService implements IKerasClassificationService {

  	constructor() { }
}
