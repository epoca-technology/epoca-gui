import { StringOverviewPipe } from './string-overview.pipe';

describe('StringOverviewPipe', () => {
  it('create an instance', () => {
    const pipe = new StringOverviewPipe();
    expect(pipe).toBeTruthy();
  });
});
