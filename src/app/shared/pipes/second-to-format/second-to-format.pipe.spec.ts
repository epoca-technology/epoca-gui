import { SecondToFormatPipe } from './second-to-format.pipe';

describe('SecondToFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondToFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
