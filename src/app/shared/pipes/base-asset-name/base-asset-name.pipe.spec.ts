import { BaseAssetNamePipe } from './base-asset-name.pipe';

describe('BaseAssetNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BaseAssetNamePipe();
    expect(pipe).toBeTruthy();
  });
});
